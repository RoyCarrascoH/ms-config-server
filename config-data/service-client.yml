server:
  port: 8080
  error:
    include-message: always
spring:
  application:
    name: service-client
  data:
    mongodb:
      database: BankClientDB
      auto-index-creation: true
      host: localhost
      port: 27017
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9093/eureka
message:
  demo: leido desde local